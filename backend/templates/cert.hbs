<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body {
      font-family: 'Roboto Mono', monospace;
      color: #000;
      padding: 2rem;
      line-height: 1.6;
    }
    .title {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 1rem;
    }
    .field {
      margin-bottom: 0.5rem;
    }
    .label {
      font-weight: bold;
      color: #444;
    }
  </style>
</head>
<body>
  <div class="title">CERT.ONE</div>
  <div class="field"><span class="label">User:</span> {{userName}}</div>
  <div class="field"><span class="label">Email:</span> {{email}}</div>
  <div class="field"><span class="label">Title:</span> {{title}}</div>
  <div class="field"><span class="label">File Name:</span> {{fileName}}</div>
  <div class="field"><span class="label">Merkle Root:</span> {{merkleRoot}}</div>
  <div class="field"><span class="label">Certificate ID:</span> {{certificateId}}</div>
  <div class="field"><span class="label">File Hash:</span> {{fileHash}}</div>
  <div class="field"><span class="label">Timestamp:</span> {{timestamp}}</div>
  <div class="field"><span class="label">Blockchain:</span> {{blockchain}}</div>
  <div class="field"><span class="label">Verify:</span> {{verificationLink}}</div>
</body>
</html>
